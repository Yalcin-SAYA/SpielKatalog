<Window x:Class="ProjektGameKatalog.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjektGameKatalog"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Border CornerRadius="15" Background="#FF3E3E42" Padding="20" Margin="20">
            <StackPanel VerticalAlignment="Center">

                <TextBlock Text="Login" FontSize="26" Foreground="White" 
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Username" Foreground="#CCCCCC" Margin="0,5,0,5"/>
                <TextBox x:Name="UsernameTextBox" Height="35" 
                         Background="#FF252526" Foreground="White" 
                         BorderBrush="#FF007ACC" BorderThickness="1"/>

                <TextBlock Text="Passwort" Foreground="#CCCCCC" Margin="0,15,0,5"/>
                <PasswordBox x:Name="PasswordBox" Height="35" 
                             Background="#FF252526" Foreground="White"
                             BorderBrush="#FF007ACC" BorderThickness="1"/>

                <Button Content="Benutzer erstellen" Height="40" Margin="0,10,0,0"
                        Background="#FF007ACC" Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="CreateUserButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="10">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>


                <Button Content="Anmelden" Height="40" Margin="0,25,0,0"
                        Background="#FF007ACC" Foreground="White"
                        BorderThickness="0" 
                        Cursor="Hand"
                        Click="LoginButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="8" 
                                                Padding="10">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</Window>
